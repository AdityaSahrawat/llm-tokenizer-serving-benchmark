[project]
name = "llm-tokenizer-serving-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.11.1",
    "tiktoken>=0.13.0",
    "transformers>=5.14.1",
]
